name: flutter_codelab
description: A new Flutter project.

publish_to: 'none'


version: 1.0.0+1

environment:
  sdk: '>=3.1.0-262.3.beta <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0
  cupertino_icons: ^1.0.2
  go_router: ^6.0.4

dev_dependencies:
  integration_test:
    sdk: flutter

  flutter_driver:
    sdk: flutter

  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  test: ^1.22.0

flutter:
  uses-material-design: true
